# Epic 4.1 — Many More Phonetic Variations — Brainstorming

> Expand the algorithmic generator with a much richer set of phonetic substitution rules and creative distortions so every word yields funnier, more varied results.
> Epic 4.1 covers:
> - More phonetic swap rules in `PHONETIC_RULES`
> - More creative techniques in `CREATIVE_TECHNIQUES`
> - Potentially generate multiple phonetic variants per word (not just the first matching rule)
> - Keep output deterministic (same input → same output)

---

## Round 1 — Scope & output shape

### Q1.1 — How many variants to return

Currently the generator returns 3 variants. Should this epic change that?

- [x] Keep 3 variants ← recommended: UI is already tuned for 3 cards
- [ ] Increase to 5 variants
- [ ] Make it configurable (caller passes a count)

> **Your answer / freetext:**
>

### Q1.2 — Multiple phonetic variants per word

Currently `makePhoneticSwap` applies **the first matching rule** and returns one variant. With many more rules there could be several applicable rules for a single word (e.g. "phone" matches both `ph→f` and `on→un`). Should we generate one phonetic variant per applicable rule?

- [x] Yes — generate one variant per matching rule, add all to the pool ← recommended: maximises variety; deduplication already in place
- [ ] No — keep first-match behaviour, just add more rules to the list
- [ ] Apply all matching rules to a single text (chain them), producing one heavily-distorted variant

> **Your answer / freetext:**
>

### Q1.3 — New variant style: `spelling_literal`

The roadmap originally called out a **spelling-literal** style ("pronounce every letter exactly as written"). This was never implemented. Should this epic add it as a third `style` value?

- [ ] Yes — add `spelling_literal` style alongside `phonetic_swap` and `creative` ← recommended: aligns with roadmap and adds a very different flavour
- [ ] No — fold spelling-literal rules into `phonetic_swap` instead
- [ ] Defer to a later epic

> **Your answer / freetext:**
> No - fold spelling-literal rules into `phonetic_swap`, but don't spell everything, just a small part of the word.

---

## Round 2 — Phonetic rule categories

The current 10 rules cover digraphs (`ph`, `th`, `ch`, etc.). We want many more. The table below groups candidate rules by category.

```
Category              | Example rules
----------------------|-----------------------------------------------
Vowel shifts          | oo→ew, ou→ow, ai→ay, ea→ee, ow→oh,
                      | a→ay, e→ee, i→eye, o→oh, u→oo
Consonant softening   | c(e/i)→s, g(e/i)→j, s→z
Consonant hardening   | g→gh, j→dj, v→b, f→ph
Digraph expansion     | x→ks, ng→ngg, mb→mm
Silent-letter reveal  | kn→ken, wr→war, gn→gun, mb→mub
                      | (expose the "silent" letter as pronounced —
                      |  the "small part of word" spelling-literal)
Double-letter collapse| ll→l, ss→s, ff→f, tt→t
Schwa insertion       | consonant clusters: "str"→"s-tuh-r", etc.
```

### Q2.1 — Which categories to include

Check all you want:

- [x] Vowel shifts ← recommended: large surface area, high comedy
- [x] Consonant softening / hardening
- [x] Digraph expansion (x→ks, ng→ngg, etc.)
- [x] Silent-letter reveal (kn→ken, wr→war) ← the "small part" spelling-literal
- [x] Double-letter collapse
- [x] Schwa insertion

> **Your answer / freetext (check all you want):**
>

### Q2.2 — Rule application: single vs. global substitution

When a rule matches multiple times in a word (e.g. three `a`s in "banana"), what should happen?

- [x] Apply globally (replace all occurrences) ← recommended: consistent with current `/g` regex approach
- [ ] First occurrence only
- [ ] Hash-chosen occurrence (deterministic, varied)

> **Your answer / freetext:**
>

### Q2.3 — Rule weighting / priority

Should some rules appear more often than others?

- [x] All rules equally weighted — random pick from all matching ← recommended: simple; volume gives variety
- [ ] Weight by "funniness tier" — tier 1 rules always enter pool first
- [ ] No weighting needed

> **Your answer / freetext:**
>

---

## Round 3 — Creative techniques expansion

The current 6 creative techniques are mostly suffix tricks (`-ington`, `-y`, `-oo`) and simple vowel/stress manipulations. More variety here means the pool stays interesting even for short words.

### Q3.1 — New suffix/prefix techniques

Which of these should be added to `CREATIVE_TECHNIQUES`?

- [x] Prefix `"mc"` → e.g. "mcbanana" ← recommended: classic silly name prefix
- [x] Suffix `"-inski"` / `"-ski"` → e.g. "banana-ski"
- [x] Suffix `"-ola"` → e.g. "bananola"
- [x] Suffix `"-inator"` → e.g. "banana-inator"
- [ ] Prefix `"el "` → e.g. "el banana"
- [x] Suffix `"-eroo"` → e.g. "bananaeroo"

> **Your answer / freetext (check all you want):**
>

### Q3.2 — New structural techniques

- [x] Reverse the whole word → e.g. "ananab" ← recommended: always produces something unique
- [x] Repeat first syllable → e.g. "ba-banana" (stutter effect)
- [ ] Swap first and last syllable
- [x] Insert `"-waka-"` in the middle
- [x] Pig Latin (`-ay` suffix, move leading consonants) → e.g. "ananabay"

> **Your answer / freetext (check all you want):**
>

### Q3.3 — Capitalisation / stress tricks (already have "vowels capitalised")

- [ ] ALL CAPS the last syllable → e.g. "baNANA" ← already partially done, this is a variant
- [ ] Alternate caps → e.g. "bAnAnA"
- [ ] Wrap in asterisks for "emphasis" → `*BANANA*` (text-only effect, no speech impact)

> **Your answer / freetext (check all you want):**
> capitalisation won't affect pronunciation.

---
